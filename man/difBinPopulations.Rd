% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/difBinPopulations.R
\name{difBinPopulations}
\alias{difBinPopulations}
\title{Per-group bin populations for a DIF item plot}
\usage{
difBinPopulations(
  match,
  group,
  num.groups = 10L,
  bin.type = c("equal-dist", "equal-freq"),
  group.names = c("Reference", "Focal")
)
}
\arguments{
\item{match}{numeric vector of the matching variable (e.g. WLE θ).}

\item{group}{numeric vector of group membership coded 0/1 (reference/focal).}

\item{num.groups}{integer: number of bins.}

\item{bin.type}{character: either \code{"equal-dist"} (default) or
\code{"equal-freq"}. See \code{\link[=plotDIFLogistic]{plotDIFLogistic()}} for details.}

\item{group.names}{character vector of length two giving names for the
reference and focal groups. Default \code{c("Reference", "Focal")}.}
}
\value{
A data frame with columns \code{Bin}, \code{Lower}, \code{Upper}, \code{Midpoint},
one \verb{n_<group>} column per group, and a \code{Flag} column that marks bins
with fewer than 30 respondents in at least one group as \code{"sparse"}.
}
\description{
Returns the sample size of each empirical bin used by \code{\link[=plotDIFLogistic]{plotDIFLogistic()}}
or \code{\link[=plotDIFOrdExpected]{plotDIFOrdExpected()}}, split by group. Useful as a diagnostic
alongside the DIF item plot — a bin with very few respondents (a common
rule of thumb is \code{n < 30} per group) should not be over-interpreted.
}
\author{
Joshua McGrane
}
