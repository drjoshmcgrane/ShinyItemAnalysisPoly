% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotDIFOrdExpected.R
\name{plotDIFOrdExpected}
\alias{plotDIFOrdExpected}
\title{Expected-score curve plot for ordinal DIF models}
\usage{
plotDIFOrdExpected(
  x,
  item = 1,
  item.name,
  group.names,
  match.name,
  draw.empirical = TRUE,
  num.groups = 3L,
  n.theta = 300L
)
}
\arguments{
\item{x}{an object of class \code{"difORD"} from \code{difNLR::difORD()}.}

\item{item}{integer or character: index or name of the item to be plotted.}

\item{item.name}{character: title for the plot. If missing, the column name
of the item in \code{x$Data} is used.}

\item{group.names}{character vector of length two: labels for the reference
and focal groups. Defaults to \code{x$group.names} (falling back to
\code{c("Reference", "Focal")}).}

\item{match.name}{character: label for the x-axis. Defaults to
\code{x$match.name}.}

\item{draw.empirical}{logical: whether to overlay observed mean item scores.
Default \code{TRUE}.}

\item{num.groups}{integer: number of equal-frequency bins to use for the
empirical overlay, mirroring the convention used by the IRT model item
plots in this package. Default \code{3}.}

\item{n.theta}{integer: number of points along the matching scale at which
to evaluate the expected-score curve. Default \code{300}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the model-implied expected item score curves for the reference and
focal groups from a polytomous DIF fit produced by \code{\link[difNLR:difORD]{difNLR::difORD()}}
(either the cumulative-logit or adjacent-category model). Optionally
overlays the empirical mean item score within equal-frequency bins of the
matching variable for each group.
}
\details{
This is the polytomous analogue of the dichotomous item characteristic curve
plot used in the IRT model tabs: each curve is the model-implied
\eqn{E[Y \mid \mathrm{match}, \mathrm{group}]} as a function of the matching
variable, on the original item-score metric (0, 1, ..., K-1).

Empirical bins are formed separately within each group using
\code{quantile()} breaks of the matching variable, and the empirical y value
is the mean of the item score within each bin. Bin sizes (counts) are
encoded via the point area.

For the cumulative-logit and adjacent-category models, expected scores are
computed as \eqn{\sum_k k \, P(Y = k \mid \mathrm{match}, \mathrm{group})}
using \code{\link[difNLR:predict.difORD]{difNLR::predict.difORD()}} with \code{type = "category"}.
}
\seealso{
\code{\link[difNLR:difORD]{difNLR::difORD()}}, \code{\link[difNLR:predict.difORD]{difNLR::predict.difORD()}}
}
\author{
Joshua McGrane, building on the difNLR DIF fitting infrastructure by
Adela Hladka and Patricia Martinkova (Institute of Computer Science, Czech
Academy of Sciences).
}
